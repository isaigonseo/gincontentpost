name: Build macOS App

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Build with PyInstaller
      run: |
        pyinstaller --noconfirm --windowed --add-data "logo.png:." --add-data "icon.ico:." --name "GinContent Post" main.py
        cd dist
        zip -ry GinContent-Post-Mac.zip "GinContent Post.app"
        
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: GinContent-Post-Mac
        path: dist/GinContent-Post-Mac.zip
